# Handover — 2026-08-01 session, for continuing via Codex CLI

Written because this Claude session is near its usage limit mid-investigation. Everything
**shipped and verified** is committed and pushed. Everything **in progress** is written down
below so nothing is lost.

## How to keep going with Codex CLI directly

You don't need a specific session — `--last` picks up the most recent one automatically:

```bash
cd /Users/roryclark/ImbewuField
codex exec resume --last "<your next instruction>"
```

To start a **fresh** Codex run on a scoped task instead of resuming, use `codex exec` directly:

```bash
cd /Users/roryclark/ImbewuField
codex exec -s workspace-write -C /Users/roryclark/ImbewuField "<prompt>"
```

**Branch:** the checkout is on `codex/2026-08-01` (created tonight so background agents never
push straight to `main`, which auto-deploys to production). It tracks `origin`. When you're happy
with what's on it, merge/fast-forward `main` yourself — I did not merge it in this session.

**Dev server:** already running on port 4343, serving whatever branch the checkout is currently
on. Don't start a second one.

## What's DONE — committed, pushed, verified (tsc clean, 1503/1503 tests)

On `codex/2026-08-01`, newest first:

1. `9ca032f` **Legend type is no longer capped by column width** — the Codex legend-typography
   run's own work, landed by me after verifying it. `legendRowFontSize` was capped at
   `columnWidth * 0.066` and never against the panel's available height, so a tall sparse legend
   (your Water-sheet screenshot) couldn't grow into its own empty space. Fixed; dense legends
   (20+ row Planting) protected by a separate, untouched mechanism.
2. `dc4720e` **Sector wind labels say WIND; overlay bolder.** The on-canvas wind arrows read
   "SUMMER ESE" with no word telling you it's wind — now "SUMMER WIND · ESE", matching what the
   printed sheet's own wind wedges already say. Whole sector overlay opacity 0.7 → 0.88 per your
   "translucent, should be bolder" note — this affects sun/wind/fire/water together, not just wind.
3. `bb6fd36` **Site sheet: staple garden no longer prints as existing fact; house/driveway get a
   map label.** The two "existing vs base map" bugs from your screenshots. Root cause:
   `buildBlueprintBaseMap` had no sheet filter of its own and reused `'all'` (meaning "the whole
   finished design"), so `staple_garden` — the one thing a farmer designs rather than records —
   read as existing content. New `existingSiteGroundRings()` in `lib/glossy-filters.ts` answers
   the real question by reusing `ownedByCurrentStep('base', ...)`, the same authority the wizard
   already uses. Applied to BOTH the free exact Site sheet and its paid Hybrid twin (they share a
   documented "must never disagree" contract). Also: the main-map-traced house/driveway never had
   an on-map label (only a legend swatch) — now gets one, in the same words a Studio-traced zone
   would use.
4. Earlier in the session (also pushed, on `main` before the branch existed):
   `59b2ca4` Earthworks sheet was printing the masterplan; `1ec415e` Earthworks is now its own
   wizard step (4 of 9); `7fa3119` zone badges honour a farmer's drag and stop overlapping;
   `4c720ea` staple garden's layer-switch ownership; `8fb8b6e` Preview map opens with the 9-sheet
   picker. Full detail in `.claude-context` at `~/Claude/.claude-context` (session `imbewufield_2026_08_01_session3`).

Docs written this session, still the best entry points:
- `docs/LEGEND-TYPOGRAPHY-BRIEF-2026-08-01.md` — now DONE, see commit above.
- `docs/EARTHWORKS-AUDIT-2026-08-01.md` — the catalog gap audit (diversion drain, level platform,
  spillway, gully repair). Not yet started.
- `docs/WORK-QUEUE-2026-08-01.md` — 10-item queue for a Codex CLI run. **Was launched but the
  process exited without committing anything** (see below) — re-launch it, or work the list
  yourself.

## What's IN PROGRESS — mid-investigation, not yet fixed

### 1. Swale rendering — "thin and scraggly", should look like a ditch + berm

Your ask: a swale is made of a ditch and a berm, should render at a real cross-section width
(auto-sized, adjustable), not as a generic thin line.

**What I found:** the machinery partly exists already. `lib/water-cartography.ts` has
`EARTHWORKS_ROUTE_STYLE.swale` — a casing colour (`#5B3A22`, dark earth) + fill colour
(`#A9743F`) + width `5.2`, clearly meant to read as a cut-and-fill earthwork rather than a plain
line. `components/design/DesignCanvas.tsx`'s `lineStroke()` (line ~330) already branches on an
`earthworksOnly` flag (`activeLayers.earthworks && !activeLayers.water`, line ~2105) to pick this
style over the plain water-route style, and the casing IS wired to actually draw (line ~2978).

**What I have NOT confirmed:** why your screenshot on the Earthworks step still shows a thin
line instead of the thicker cased one. I was mid-way checking whether `activeLayers.water` is
somehow still `true` alongside `earthworks` in your session (a stale Layers toggle from before the
step existed, or `applyStepFocus` not landing the way I think) when this session had to stop.
**Next step:** in the browser, on the Earthworks step, open Layers and check whether Water is
also switched on — if so, that's the bug (the two should be mutually exclusive there per
`earthworksOnly`'s own formula, and if a farmer's stale toggle can defeat that, the guard is
wrong). If Water is genuinely off and it's STILL thin, the bug is somewhere in `lineStroke` or in
`chrome()` scaling, not in the flag.

**Also still open, not investigated:** `LineShape` has no width field at all — there is currently
no way to size a swale differently from another, "auto-sized to an average, adjustable if needed"
as you asked. That's a real data-model addition (similar in shape to `ZoneShape.levelM` for
terraces), not a quick tweak.

**Note found in the code while investigating** — `lib/water-cartography.ts` line ~71 says the
earthworks sheet "does not yet register swale as its content" (a `layerContentCount`/
`groundRegister`-adjacent gap). I did NOT verify whether my Earthworks-step work earlier tonight
already fixed this incidentally — check before assuming either way.

### 2. Earthworks ⇄ Planting cross-linking audit (your big multi-part question)

You asked: where should raised beds be encouraged (Earthworks/Planting/both)? What happens to
existing beds — Base map too? Do tree basins auto-produce when a tree is placed? Does the "brown
earthworks duplicate" of a raised bed really "transform to have veg" in Planting view? Should
linkage go both ways?

**What I confirmed before running out of turn:**
- The mechanism you're half-remembering is real and already built: `DesignElementDef.alsoLayers`
  (`lib/design-elements.ts` ~line 44-59) — its own doc comment quotes your exact earlier feedback
  ("earthworks should auto show raised beds in brown earth, auto mirroring where we put the raised
  beds in planting"). One item, multiple layer memberships; each renderer decides how to draw it.
- `raised_bed`'s definition: `category: 'earthworks'` (so it's offered from the Water/Earthworks
  palette, not Planting) with `alsoLayers: ['earthworks']`. **This line may be a no-op bug** — if
  its category is ALREADY `'earthworks'`, adding `'earthworks'` again via `alsoLayers` does
  nothing. I had NOT yet confirmed this against `SHEET_OVERRIDE` (`lib/glossy-filters.ts` ~line
  299) when the session ended — I strongly suspect `SHEET_OVERRIDE['raised_bed']` sends its
  PRINTED sheet to `'planting'` (a comment elsewhere in the file says exactly this: "raised
  bed/keyhole bed/herb spiral/tree basin ... SHEET_OVERRIDE'd onto the Planting sheet"), which
  would mean `alsoLayers: ['earthworks']` is the thing that ADDS the earthworks sheet as a SECOND
  destination on top of that — i.e., it may be correct and NOT a bug. **Verify
  `SHEET_OVERRIDE['raised_bed']` before touching this.**
- `tree_basin` has NO `alsoSteps`/`alsoLayers` at all. I found no code anywhere that auto-creates
  a tree basin when a tree item is placed — I believe your "tree basins are automatically
  produced where a tree is inserted" describes a WISH, not current behaviour, but I did not
  finish a targeted search to be certain. Grep `tree_basin` against every tree-placement code path
  (`runTapAction` in `DesignCanvas.tsx`, `onPlaceBedBlock`) before concluding either way.
- Not yet investigated at all: whether an EXISTING (already-there) raised bed should also register
  on the Base/existing-site sheet, the same way `existingSiteGroundRings` now handles ground
  ZONES. Raised beds are ITEMS, not zones, so `ElementStatus` (`'existing' | 'proposed'`,
  `lib/design-canvas.ts`) is probably the right lever — an item's `status` field already exists
  for this, check whether the Base sheet reads it for items at all today.

**Recommended next step:** this is a real, well-scoped audit task. Hand it to a fresh Codex run
with this exact question and the pointers above — don't let it guess, make it read
`alsoLayers`'s doc comment, `SHEET_OVERRIDE`, and `CATEGORY_STEP` first, then report before
changing anything.

### 3. Smaller open items, not started

- **"What is a terrace / retaining bank"** — you asked live in the app. Quick answer for now:
  it's the RISER face between two levels you've cut into a slope (the vertical/sloped wall of a
  terrace step), not the flat part you'd stand a bed on — see the catalog tip text on the chip
  itself, and `docs/EARTHWORKS-AUDIT-2026-08-01.md`'s note that the flat PLATFORM half of this
  concept doesn't exist yet as its own traceable shape.
- **Seasonal sun-angle panel "at the top"** — you asked me to research the best name for this.
  Not researched yet. Worth 15 minutes with a permaculture/architecture glossary before naming it
  in the UI — candidates to start from: "Sun Path", "Solar Access", "Seasonal Sun Angles".
- **Floating tool modal "on all layers"** — referenced an earlier promised feature. Not built,
  not scoped. Needs a clarifying pass on what "float" means here before anyone codes it.
- **Sector sliders** (arrow size / font / opacity) — you said "I dunno" yourself; treat as a loose
  idea, not a spec, until you or I firm it up.
- **Should Earthworks own NEW driveway/path construction, not just swales/terraces?** You asked
  live. Today `access` (driveway, gates, paths) is category `'access'`, owned by the Structures
  step (`CATEGORY_TO_LAYER` in `app/design/page.tsx`) — that's fine for a driveway that already
  exists (traced on Base) or a finished path, but CUTTING a new driveway/path into a slope is
  earthworks (cut-and-fill grading) exactly like a terrace is. My instinct: this wants the SAME
  `alsoLayers`/`alsoSteps` treatment as `raised_bed` — access stays owned by Structures (where a
  farmer expects to find "add a path"), but a NEW graded driveway/path gets `alsoLayers:
  ['earthworks']` so its earthwork exists on the Earthworks sheet too. Not investigated further —
  add to the same audit as raised-bed/tree-basin above rather than deciding in isolation.

## Standing rules for whoever continues this (Codex or otherwise)

- Free exact renders only for verification. Never trigger a paid AI render.
- Never touch `PLAN_VERSION`. Never invent a number that reads as an agronomic recommendation.
- `npx tsc --noEmit` and `npm test` must both pass. 1503 pass right now — that number must not
  drop.
- Never `git add -A` / `git add .` in this shared checkout.
- Stay on `codex/2026-08-01`. Never push to `main`.
