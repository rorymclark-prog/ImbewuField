# Soil lesson 3 review

This is an unpublished comparison, not the production course. It uses unchanged
lesson text, quizzes, English narration and existing media. No learner data is saved.

Run `python3 restore.py` here to reconstruct the media from the repository and
recovered Downloads originals. Every source must match its recorded SHA-256.
Then serve this directory with `python3 -m http.server 4372 --bind 127.0.0.1`.
The original complete review and a ZIP backup are also in
`/Users/roryclark/Downloads/ImbewuField-Animation-Quality-2026-09-22/`.

The video tabs compare published, existing Fast and latest Quality clips.
Use **Play current slide** for narration alongside the selected clip, or the
whole-lesson button to advance automatically. **Read slides without audio**
opens the stills with manual navigation. Slides 14–18 form lesson 3;
19–20 are optional module wrap-up. Flow candidates remain unaccepted.
