# Vegetables and Staple Crops deck — 20 September 2026

The module now has 18 English slides, generated by the existing lesson-slide renderer from its unchanged English narration and paired with the existing 18 recordings. Its authored script has no Watch sections, so no animation slots were invented. The title uses the module's original bed-preparation illustration, padded to preserve the whole image. No lesson, quiz, narration, species, farming figure, geometry or PLAN_VERSION changed; isiZulu remains an unpublished review draft.

This branch follows Soil Health (#449), Reading the Landscape (#448), Introduction (#447) and Water (#446). The complete contact sheet was visually inspected. All 18 original MP3s fully decoded, and `verification.json` records source/audio/image hashes and image byte counts. Images are 1920×1080 JPEGs at quality 85. The generated size index supplies the existing offline download calculation.

Reproduction uses the existing script:

```sh
node scripts/make-lesson-slides.mjs vegetables-staples en OUTPUT --images ART
```

Supply the original lesson-one art as `ART/slide-01.jpg`, padded to 1076×1080. Convert resulting PNGs to JPEG at quality 85 for the app. The local review pack is `Downloads/imbewu-studies-2026-09-20/vegetables-production/`.

The local app displayed the registered deck with its title image at 1920×1080 and played the original narration at readyState 4 with advancing time.

No physical-device or fluent-language sign-off is claimed. Deployed disconnected playback is checked after the branch preview builds.

Local `npx tsc --noEmit`, the full suite (3,632 pass, zero failures, one existing TODO) and `git diff --check` passed in order.
