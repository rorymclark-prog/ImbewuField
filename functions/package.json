{
  "name": "functions",
  "description": "ImbewuField Cloud Functions — background AI render worker (direct OpenAI gpt-image, escapes Vercel's 60s limit).",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/functions/src/index.js",
  "scripts": {
    "build": "tsc && node ./verify-entry.mjs",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.0",
    "pngjs": "^7.0.0"
  },
  "devDependencies": {
    "@types/pngjs": "^6.0.5",
    "typescript": "^5.6.3"
  }
}
