.next/
node_modules/
.env.local
out/
.vercel
serviceAccount.json

.env*
!.env.example
!.env.emulator.example
.vercel
*.tsbuildinfo

# Firebase emulator debug logs (created whenever `firebase emulators:start` runs)
firebase-debug.log
firestore-debug.log
ui-debug.log

# Dev-only plan-sheet captures (app/api/dev-capture) — evidence images, never source.
.sheet-captures/
.playwright-mcp/

# Claude Code agent worktrees (in-progress agent workspaces — never commit)
.claude/worktrees/

# Generated isiZulu review packets. Derived from docs/narration/<module>.{en,zu}.md by
# `npm run course:review-packet` — a committed copy would drift from the scripts it summarises,
# and the reviewer's answers belong in the .zu.md source, not in a generated file.
docs/narration/review/
